<h2>{{#str}} reflection-exporter-heading, report_reflectionexporter {{/str}}</h2>

<div class="actions">
   <a class="action btn btn-lg" href="{{{newproc}}}">
      <img class="icon iconsize-big" alt="" aria-hidden="true" src="{{{newicon}}}" />
      <div class="action-text">{{#str}} action-text-new, report_reflectionexporter {{/str}}</div>
   </a>
</div>

{{#processfound}}
    <div class = "select-work">
    <h4>Select started work</h4>
    <br>
    <table class="table table-bordered table-responsive started-work">
    <thead>
    <tr>
        <th>Date Created</th>
        <th>Status</th>
        <th></th>
        <th></th>
    </tr>
    </thead>
    <tbody>
    {{#processes}}
        <tr>
            <td>{{datecreated}}</td> 
            <td>{{status}}</td> 
            <td><a type="button" class="btn " href = "{{{actionurl}}}" title = '{{title}}'>
                <img class="icon reflection-exporter-action-btn" alt="" aria-hidden="true" src="{{{existingicon}}}" /></a> 
            </td>
            <td>
                <a type="button" class="btn " title = 'Delete' >
                <img class="icon reflection-exporter-action-btn" alt="" aria-hidden="true" src="{{{deleteicon}}}" data-to-delete = "{{todelete}}" />
                </a> 
                <div class = 'deleting del-processing'>
                    {{> report_reflectionexporter/loading }}
                </div>
            </td>
        </tr>
    {{/processes}}
    </tbody>
    </table>
    </div>
{{/processfound}}

{{#js}}
require(['report_reflectionexporter/delete'], function(Delete) {
  new Delete()
});
{{/js}}